{
    "name": "@replyguy/ai-agent",
    "version": "0.0.0",
    "private": true,
    "description": "ReplyGuy AI Agent Python service",
    "scripts": {
        "dev": "poetry run uvicorn main:app --reload --port ${AI_AGENT_PORT:-8001}",
        "start": "poetry run uvicorn main:app --port ${AI_AGENT_PORT:-8001}",
        "setup": "poetry install",
        "test": "poetry run pytest",
        "lint": "poetry run ruff check .",
        "format": "poetry run ruff format ."
    },
    "devDependencies": {
        "@replyguy/eslint-config": "workspace:*",
        "@replyguy/typescript-config": "workspace:*"
    }
}