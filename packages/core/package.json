{
    "name": "@replyguy/core",
    "version": "0.1.0",
    "private": true,
    "description": "Core utilities, types, and logging for ReplyGuy",
    "main": "dist/index.js",
    "types": "dist/index.d.ts",
    "files": [
        "dist"
    ],
    "scripts": {
        "build": "tsup && tsc --emitDeclarationOnly",
        "dev": "tsup --watch",
        "check-types": "tsc --noEmit",
        "lint": "eslint src/",
        "clean": "rm -rf dist"
    },
    "dependencies": {
        "pino": "^8.16.0",
        "zod": "^3.22.4"
    },
    "devDependencies": {
        "@replyguy/eslint-config": "workspace:*",
        "@replyguy/typescript-config": "workspace:*",
        "eslint": "^9.28.0",
        "tsup": "^8.0.1",
        "typescript": "^5.2.2"
    }
}